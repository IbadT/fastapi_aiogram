# from api.schemas.database import async_session

async def register_user(telegram_id) -> None:
    # async with async_session() as session:
        # return await session.scalars(select(Category))

    print("Register User")
