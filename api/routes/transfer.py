# from fastapi import APIRouter, Depends, HTTPException
# from .. import schemas
#
# router = APIRouter()
#
# @router.post("/transfer", response_model=schemas.Transfer)
# def transfer_funds(transfer: schemas.Transfer):
#     # Логика перевода средств
#     return {"status": "success"}
